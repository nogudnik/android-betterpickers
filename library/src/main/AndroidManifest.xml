<?xml version="1.0" encoding="utf-8"?>

<manifest package="com.doomonafireball.betterpickers">
    <!-- versionCode, versionName and minSdkVersion all come from teh gradle build script. -->

    <!-- The library checks at runtime for this permission. If you would like to
         use haptic feedback in the widgets (provided it is enabled on the device)
         then your app needs to request this permission.
    <uses-permission android:name="android.permission.VIBRATE" />
    -->
</manifest>
